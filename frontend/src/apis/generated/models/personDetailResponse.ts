/**
 * Generated by orval v7.9.0 🍺
 * Do not edit manually.
 * 관계도감 API
 * 관계도감 — 사람 중심 관계 기록 서비스의 백엔드 API.

사람(인물)·기록·칩(카테고리·감정·날씨·관계태그)을 중심으로 관계 지도, 활동 흐름, 나의 통합 연대기(전체 타임라인), 1년 전 오늘 회고를 제공한다.

**인증**: `POST /api/v1/auth/token` 으로 발급받은 JWT 를 우측 상단 Authorize 에 넣으면 이후 모든 API 에 `Authorization: Bearer {token}` 이 붙는다.
 * OpenAPI spec version: v1
 */
import type { Birthday } from './birthday'
import type { PersonDetailResponseGender } from './personDetailResponseGender'
import type { ChipRef } from './chipRef'
import type { PersonStats } from './personStats'

/**
 * 인물 상세 응답. 기본 정보에 파생 스탯 섹션을 더한다. lastMetDate 는 수기+기록 max 재계산값.
 */
export interface PersonDetailResponse {
  /** 인물 id. */
  id: number
  /** 인물 이름. */
  name: string
  birthday?: Birthday
  /**
   * 처음 만난 날(없을 수 있음).
   * @nullable
   */
  firstMetDate?: string | null
  /**
   * 마지막 만난 날. 수기 입력과 기록의 max 를 재계산한 값.
   * @nullable
   */
  lastMetDate?: string | null
  /**
   * 프로필 이미지 URL(없을 수 있음).
   * @nullable
   */
  profileImageUrl?: string | null
  /**
   * 기본 아바타 선택용 성별 힌트.
   * @nullable
   */
  gender?: PersonDetailResponseGender
  /**
   * 관계 유형(없을 수 있음).
   * @nullable
   */
  relationType?: string | null
  /** 관계태그 칩 요약 참조 목록. */
  relationTags: ChipRef[]
  /** 좋아하는 것 목록. */
  likes: string[]
  /** 주의할 것 목록. */
  cautions: string[]
  /** 즐겨찾기 여부. */
  favorite: boolean
  /**
   * 등록 시각.
   * @nullable
   */
  createdAt?: string | null
  stats: PersonStats
}
